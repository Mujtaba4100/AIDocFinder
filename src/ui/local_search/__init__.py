"""Local (desktop) indexing + search utilities for DocuFindAI.

This package is intentionally backend-agnostic.
It keeps raw files local and stores only metadata + embeddings in a local SQLite DB.
"""
